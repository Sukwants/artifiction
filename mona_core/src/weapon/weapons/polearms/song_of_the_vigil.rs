// Generated by GitHub Copilot (DeepSeek V4 Flash)
use crate::weapon::weapons::prelude::*;

pub struct SongOfTheVigilEffect {
    pub stellar_reaction: bool,
}

impl<A: Attribute> WeaponEffect<A> for SongOfTheVigilEffect {
    fn apply(&self, data: &WeaponCommonData, attribute: &mut A) {
        let refine = data.refine as f64;

        // 触发星烁反应的12秒内，装备者的攻击力提升。
        // （触发元素反应后恢复元素能量的效果不参与伤害计算，故不实现。）
        if self.stellar_reaction {
            attribute.add_atk_percentage("戍望谣歌被动", 0.15 + 0.05 * refine);
        }
    }
}

pub struct SongOfTheVigil;

impl WeaponTrait for SongOfTheVigil {
    const META_DATA: WeaponStaticData = WeaponStaticData {
        name: WeaponName::SongOfTheVigil,
        internal_name: "Polearm_GlintstonePolearm",
        weapon_type: WeaponType::Polearm,
        weapon_sub_stat: Some(WeaponSubStatFamily::EM24),
        weapon_base: WeaponBaseATKFamily::ATK565,
        star: 4,
        #[cfg(not(target_family = "wasm"))]
        effect: Some(crate::common::i18n::locale!(
            zh_cn: "触发元素反应后，为装备者恢复 <span style=\"color: #409EFF;\">4-5-6-7-8</span> 点元素能量，该效果每9秒至多触发一次；触发星烁反应的12秒内，装备者的攻击力提升 <span style=\"color: #409EFF;\">20%-25%-30%-35%-40%</span> 。装备者处于队伍后台时，依然能触发上述效果。",
            en: "Triggering an Elemental Reaction regenerates <span style=\"color: #409EFF;\">4-5-6-7-8</span> Elemental Energy for the equipping character. This effect can trigger once every 9s. On the other hand, triggering a Stellar Glimmer reaction increases their ATK by <span style=\"color: #409EFF;\">20%-25%-30%-35%-40%</span> for 12s. The aforementioned effects can trigger even when the character is not on the field."
        )),
        #[cfg(not(target_family = "wasm"))]
        name_locale: crate::common::i18n::locale!(
            zh_cn: "戍望谣歌",
            en: "Song of the Vigil"
        )
    };

    #[cfg(not(target_family = "wasm"))]
    const CONFIG_DATA: Option<&'static [ItemConfig]> = Some(&[
        ItemConfig {
            name: "stellar_reaction",
            title: locale!(zh_cn: "触发星烁反应", en: "Trigger Stellar Glimmer Reaction"),
            config: ItemConfigType::Bool { default: true },
        },
    ]);

    fn get_effect<A: Attribute>(character: &CharacterCommonData, config: &WeaponConfig) -> Option<Box<dyn WeaponEffect<A>>> {
        let stellar_reaction = match *config {
            WeaponConfig::SongOfTheVigil { stellar_reaction } => stellar_reaction,
            _ => false,
        };

        Some(Box::new(SongOfTheVigilEffect {
            stellar_reaction,
        }))
    }
}
