// Generated by GitHub Copilot (DeepSeek V4 Flash)
use crate::weapon::weapons::prelude::*;

pub struct FrostbreathEffect {
    pub reaction: bool,
}

impl<A: Attribute> WeaponEffect<A> for FrostbreathEffect {
    fn apply(&self, data: &WeaponCommonData, attribute: &mut A) {
        let refine = data.refine as f64;

        // 触发冰元素或水元素相关反应后的15秒内，装备者的攻击力提升。
        // 战斗过程中相关反应持续触发，该效果近乎常驻，故以布尔配置项表示是否触发。
        // （为队伍中其他角色恢复元素能量的效果不参与伤害计算，故不实现。）
        if self.reaction {
            attribute.add_atk_percentage("寒息被动", 0.15 + 0.05 * refine);
        }
    }
}

pub struct Frostbreath;

impl WeaponTrait for Frostbreath {
    const META_DATA: WeaponStaticData = WeaponStaticData {
        name: WeaponName::Frostbreath,
        internal_name: "Polearm_FaesCrystalle",
        weapon_type: WeaponType::Polearm,
        weapon_sub_stat: Some(WeaponSubStatFamily::Recharge100),
        weapon_base: WeaponBaseATKFamily::ATK510,
        star: 4,
        #[cfg(not(target_family = "wasm"))]
        effect: Some(crate::common::i18n::locale!(
            zh_cn: "触发冰元素或水元素相关反应后的15秒内，装备者的攻击力提升 <span style=\"color: #409EFF;\">20%-25%-30%-35%-40%</span> ，并为队伍中自己的其他角色恢复 <span style=\"color: #409EFF;\">6-7.5-9-10.5-12</span> 点元素能量。该效果每16秒至多触发一次。",
            en: "Triggering a Cryo or Hydro-related elemental reaction increases the equipping character's ATK by <span style=\"color: #409EFF;\">20%-25%-30%-35%-40%</span> for the next 15s, as well as regenerates <span style=\"color: #409EFF;\">6-7.5-9-10.5-12</span> Elemental Energy for other members of their party. This effect can trigger once every 16s."
        )),
        #[cfg(not(target_family = "wasm"))]
        name_locale: crate::common::i18n::locale!(
            zh_cn: "寒息",
            en: "Frostbreath"
        )
    };

    #[cfg(not(target_family = "wasm"))]
    const CONFIG_DATA: Option<&'static [ItemConfig]> = Some(&[
        ItemConfig {
            name: "reaction",
            title: locale!(zh_cn: "触发冰/水相关反应", en: "Trigger Cryo/Hydro-related Reaction"),
            config: ItemConfigType::Bool { default: true },
        },
    ]);

    fn get_effect<A: Attribute>(character: &CharacterCommonData, config: &WeaponConfig) -> Option<Box<dyn WeaponEffect<A>>> {
        let reaction = match *config {
            WeaponConfig::Frostbreath { reaction } => reaction,
            _ => false,
        };

        Some(Box::new(FrostbreathEffect {
            reaction,
        }))
    }
}
