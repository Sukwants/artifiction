// Generated by GitHub Copilot (DeepSeek V4 Flash)
use crate::weapon::weapons::prelude::*;

pub struct CovenantOfFrostAndSnowEffect {
    pub rate: f64,
}

impl<A: Attribute> WeaponEffect<A> for CovenantOfFrostAndSnowEffect {
    fn apply(&self, data: &WeaponCommonData, attribute: &mut A) {
        let refine = data.refine as f64;

        // 装备者施放元素战技后的12秒内，元素精通提升。
        // 持续时间随角色循环变化，以「应用比例」配置项近似平均覆盖率。
        if self.rate > 0.0 {
            attribute.set_value_by(AttributeName::ElementalMastery, "霜雪誓约被动", (90.0 + 30.0 * refine) * self.rate);
        }
    }
}

pub struct CovenantOfFrostAndSnow;

impl WeaponTrait for CovenantOfFrostAndSnow {
    const META_DATA: WeaponStaticData = WeaponStaticData {
        name: WeaponName::CovenantOfFrostAndSnow,
        internal_name: "Bow_GlintstoneBow",
        weapon_type: WeaponType::Bow,
        weapon_sub_stat: Some(WeaponSubStatFamily::DEF113),
        weapon_base: WeaponBaseATKFamily::ATK510,
        star: 4,
        #[cfg(not(target_family = "wasm"))]
        effect: Some(crate::common::i18n::locale!(
            zh_cn: "装备者施放元素战技后的12秒内，元素精通提升 <span style=\"color: #409EFF;\">120-150-180-210-240</span> 点。",
            en: "For 12s after the equipping character uses an Elemental Skill, their Elemental Mastery is increased by <span style=\"color: #409EFF;\">120-150-180-210-240</span>."
        )),
        #[cfg(not(target_family = "wasm"))]
        name_locale: crate::common::i18n::locale!(
            zh_cn: "霜雪誓约",
            en: "Covenant of Frost and Snow"
        )
    };

    #[cfg(not(target_family = "wasm"))]
    const CONFIG_DATA: Option<&'static [ItemConfig]> = Some(&[
        ItemConfig {
            name: "rate",
            title: ItemConfig::DEFAULT_RATE_TITLE,
            config: ItemConfigType::Float { min: 0.0, max: 1.0, default: 1.0 },
        },
    ]);

    fn get_effect<A: Attribute>(character: &CharacterCommonData, config: &WeaponConfig) -> Option<Box<dyn WeaponEffect<A>>> {
        let rate = match *config {
            WeaponConfig::CovenantOfFrostAndSnow { rate } => rate,
            _ => 0.0,
        };

        Some(Box::new(CovenantOfFrostAndSnowEffect {
            rate,
        }))
    }
}
