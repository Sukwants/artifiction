// Generated by GitHub Copilot (DeepSeek V4 Flash)
use crate::weapon::weapons::prelude::*;

pub struct EmberwellEffect {
    pub element_reaction: bool,
    pub stellar_reaction: bool,
}

impl<A: Attribute> WeaponEffect<A> for EmberwellEffect {
    fn apply(&self, data: &WeaponCommonData, attribute: &mut A) {
        let refine = data.refine as f64;

        // 触发元素反应的12秒内，装备者的攻击力提升。
        if self.element_reaction {
            attribute.add_atk_percentage("引火之源被动", 0.12 + 0.04 * refine);
        }

        // 触发星烁反应的12秒内，装备者造成的星烁反应伤害提升（星超导/星扩散）。
        if self.stellar_reaction {
            attribute.set_value_to_t(AttributeType::Invisible(InvisibleAttributeType::new_reaction(
                AttributeVariableType::ReactionEnhance,
                ReactionType::StellarConduct,
            )), "引火之源被动", 0.12 + 0.04 * refine);
            attribute.set_value_to_t(AttributeType::Invisible(InvisibleAttributeType::new_reaction(
                AttributeVariableType::ReactionEnhance,
                ReactionType::StellarSwirl,
            )), "引火之源被动", 0.12 + 0.04 * refine);
        }
    }
}

pub struct Emberwell;

impl WeaponTrait for Emberwell {
    const META_DATA: WeaponStaticData = WeaponStaticData {
        name: WeaponName::Emberwell,
        internal_name: "Sword_GlintstoneSword",
        weapon_type: WeaponType::Sword,
        weapon_sub_stat: Some(WeaponSubStatFamily::EM36),
        weapon_base: WeaponBaseATKFamily::ATK510,
        star: 4,
        #[cfg(not(target_family = "wasm"))]
        effect: Some(crate::common::i18n::locale!(
            zh_cn: "触发元素反应的12秒内，装备者的攻击力提升 <span style=\"color: #409EFF;\">16%-20%-24%-28%-32%</span> ；触发星烁反应的12秒内，装备者造成的星烁反应伤害提升 <span style=\"color: #409EFF;\">16%-20%-24%-28%-32%</span> 。装备者处于队伍后台时，依然能触发上述效果。",
            en: "Triggering an Elemental Reaction increases the equipping character's ATK by <span style=\"color: #409EFF;\">16%-20%-24%-28%-32%</span> for 12s. Triggering a Stellar Glimmer reaction increases their Stellar Glimmer reaction DMG dealt by <span style=\"color: #409EFF;\">16%-20%-24%-28%-32%</span> for 12s. The aforementioned effects can trigger even when the character is not on the field."
        )),
        #[cfg(not(target_family = "wasm"))]
        name_locale: crate::common::i18n::locale!(
            zh_cn: "引火之源",
            en: "Emberwell"
        )
    };

    #[cfg(not(target_family = "wasm"))]
    const CONFIG_DATA: Option<&'static [ItemConfig]> = Some(&[
        ItemConfig {
            name: "element_reaction",
            title: locale!(zh_cn: "触发元素反应", en: "Trigger Elemental Reaction"),
            config: ItemConfigType::Bool { default: true },
        },
        ItemConfig {
            name: "stellar_reaction",
            title: locale!(zh_cn: "触发星烁反应", en: "Trigger Stellar Glimmer Reaction"),
            config: ItemConfigType::Bool { default: true },
        },
    ]);

    fn get_effect<A: Attribute>(character: &CharacterCommonData, config: &WeaponConfig) -> Option<Box<dyn WeaponEffect<A>>> {
        let (element_reaction, stellar_reaction) = match *config {
            WeaponConfig::Emberwell { element_reaction, stellar_reaction } => (element_reaction, stellar_reaction),
            _ => (false, false),
        };

        Some(Box::new(EmberwellEffect {
            element_reaction,
            stellar_reaction,
        }))
    }
}
