// Generated by GitHub Copilot (DeepSeek V4.1 Flash)
use crate::weapon::weapons::prelude::*;
use crate::common::StellarGlimmerState;

pub struct NewBoughEffect {
    pub stack: f64,
    pub stellar_glimmer_state: StellarGlimmerState,
}

impl<A: Attribute> WeaponEffect<A> for NewBoughEffect {
    fn apply(&self, data: &WeaponCommonData, attribute: &mut A) {
        let refine = data.refine as f64;

        // 施放元素战技后的 12 秒内，装备者的攻击命中敌人时获得「蓊郁」（持续 6 秒，至多 3 层，
        // 每 1 秒至多触发一次），以「被动等效层数」近似其平均层数。
        if self.stellar_glimmer_state.is_stellar_glimmer() {
            // 辉映·星烁：「蓊郁」效果改为攻击力提升，且装备者造成的星烁反应伤害提升
            // （星烁反应包含星超导与星扩散）。
            let bonus = (0.06 + 0.02 * refine) * self.stack;
            attribute.add_atk_percentage("新枝被动", (0.045 + 0.015 * refine) * self.stack);
            attribute.set_value_to_t(
                AttributeType::Invisible(InvisibleAttributeType::new_reaction(
                    AttributeVariableType::ReactionEnhance,
                    ReactionType::StellarConduct,
                )),
                "新枝被动",
                bonus,
            );
            attribute.set_value_to_t(
                AttributeType::Invisible(InvisibleAttributeType::new_reaction(
                    AttributeVariableType::ReactionEnhance,
                    ReactionType::StellarSwirl,
                )),
                "新枝被动",
                bonus,
            );
        } else {
            // 「蓊郁」：攻击力提升、元素精通提升。
            attribute.add_atk_percentage("新枝被动", (0.03 + 0.01 * refine) * self.stack);
            attribute.set_value_to(AttributeName::ElementalMastery, "新枝被动", (15.0 + 5.0 * refine) * self.stack);
        }
    }
}

pub struct NewBough;

impl WeaponTrait for NewBough {
    const META_DATA: WeaponStaticData = WeaponStaticData {
        name: WeaponName::NewBough,
        internal_name: "Sword_SpikedStake",
        weapon_type: WeaponType::Sword,
        weapon_sub_stat: Some(WeaponSubStatFamily::CriticalDamage120),
        weapon_base: WeaponBaseATKFamily::ATK510,
        star: 4,
        #[cfg(not(target_family = "wasm"))]
        effect: Some(crate::common::i18n::locale!(
            zh_cn: "施放元素战技后的12秒内，装备者的攻击命中敌人时，会获得「蓊郁」：攻击力提升 <span style=\"color: #409EFF;\">4%-5%-6%-7%-8%</span> ，元素精通提升 <span style=\"color: #409EFF;\">20-25-30-35-40</span> 点。该效果持续6秒，至多叠加3层，每1秒至多触发一次。装备者处于队伍后台时，依然能触发上述效果。<br>辉映·星烁：「蓊郁」效果改为攻击力提升 <span style=\"color: #409EFF;\">6%-7.5%-9%-10.5%-12%</span> ，且装备者造成的星烁反应伤害提升 <span style=\"color: #409EFF;\">8%-10%-12%-14%-16%</span> 。",
            en: "When the equipping character hits the opponent with an attack within 12s after using the Elemental Skill, they gain the \"Verdant\" effect, which increases their ATK by <span style=\"color: #409EFF;\">4%-5%-6%-7%-8%</span> and their Elemental Mastery by <span style=\"color: #409EFF;\">20-25-30-35-40</span>. This effect lasts 6s and can trigger once every second. Max 3 stacks. The aforementioned effects can still trigger even when the equipping character is not on the field.<br>Radiance: Stellar Glimmer: The effect of \"Verdant\" is changed to: Increases ATK by <span style=\"color: #409EFF;\">6%-7.5%-9%-10.5%-12%</span> as well as Stellar Glimmer reaction DMG dealt by the equipping character by <span style=\"color: #409EFF;\">8%-10%-12%-14%-16%</span>."
        )),
        #[cfg(not(target_family = "wasm"))]
        name_locale: crate::common::i18n::locale!(
            zh_cn: "新枝",
            en: "New Bough"
        )
    };

    #[cfg(not(target_family = "wasm"))]
    const CONFIG_DATA: Option<&'static [ItemConfig]> = Some(&[
        ItemConfig {
            name: "stack",
            title: ItemConfig::DEFAULT_STACK_TITLE,
            config: ItemConfigType::Float { min: 0.0, max: 3.0, default: 3.0 },
        },
        ItemConfig::STELLAR_GLIMMER_STATE(StellarGlimmerState::None, ItemConfig::PRIORITY_WEAPON),
    ]);

    fn get_effect<A: Attribute>(character: &CharacterCommonData, config: &WeaponConfig) -> Option<Box<dyn WeaponEffect<A>>> {
        let (stack, stellar_glimmer_state) = match *config {
            WeaponConfig::NewBough { stack, stellar_glimmer_state } => (stack, stellar_glimmer_state),
            _ => (0.0, StellarGlimmerState::None),
        };

        Some(Box::new(NewBoughEffect {
            stack,
            stellar_glimmer_state,
        }))
    }
}
